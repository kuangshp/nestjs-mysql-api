import { PaginateInterceptor } from './paginate.interceptor';

describe('PaginateInterceptor', () => {
  it('should be defined', () => {
    expect(new PaginateInterceptor()).toBeDefined();
  });
});
